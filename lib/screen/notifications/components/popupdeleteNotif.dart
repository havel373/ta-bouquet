import 'package:flutter/material.dart';

class popupdeleterNotif extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        'Halo, ini Stateless Widget!',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}